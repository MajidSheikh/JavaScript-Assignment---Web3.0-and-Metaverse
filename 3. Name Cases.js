let name = "majid";
let upperCase = name.toLowerCase();
let lowerCase = name.toUpperCase();
let titleCase = (name.charAt(0).toUpperCase()).concat(name.substring(1));

console.log(`Lower case: ${upperCase} \nUpper case: ${lowerCase} \nTitle case: ${titleCase} `);